<h1>This is the show page</h1>
<table>
  <tr>
    <th>Title</th>
    <th>Body</th>
  </tr>
  <tr>
<td><%= @blog.title %></td>
<td><%= @blog.body %></td>
<td><%= link_to 'Destroy', blog_path(@blog), method: :delete, data: { confirm: "Are you sure you want to remove this?"} %></td>
  </tr>
</table>