<%= form_for @state do |f| %>
  <div class="card">
    <form>
      <div class="form-group">
        <%= f.label :name %>
        <%= f.text_field :name %>
        <%= f.collection_select(:country_id, Country.all, :id, :name) %>
        <%= f.submit %>
      </div>
    </form>
  </div>
<% end %>